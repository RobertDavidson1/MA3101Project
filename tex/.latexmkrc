$cleanup_mode = 2;  # Remove all generated files except for the final PDF

$clean_ext = "aux bbl bcf blg idx ilg ind lof log lot out run.xml synctex.gz toc acn acr alg glg glo gls ist fls fdb_latexmk nav snm vrb bcf synctex.gz(busy)";

# Force cleanup mode
$cleanup_includes_generated = 1;

# Ensure cleanup happens
$clean_full_ext = 1;